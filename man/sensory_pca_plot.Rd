% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensory_pca_plot.R
\name{sensory_pca_plot}
\alias{sensory_pca_plot}
\title{Plot a sensory PCA map}
\usage{
sensory_pca_plot(
  x,
  type = c("biplot", "scores", "loadings"),
  pc_x = 1,
  pc_y = 2,
  label_products = TRUE,
  label_attributes = TRUE,
  vector_scale = NULL,
  show_origin = TRUE
)
}
\arguments{
\item{x}{An object of class `sensory_pca`.}

\item{type}{Character. Plot type. One of `"biplot"`, `"scores"`,
or `"loadings"`. Default is `"biplot"`.}

\item{pc_x}{Integer. Principal component for the horizontal axis.
Default is 1.}

\item{pc_y}{Integer. Principal component for the vertical axis.
Default is 2.}

\item{label_products}{Logical. If TRUE, product labels are displayed.
Default is TRUE.}

\item{label_attributes}{Logical. If TRUE, sensory attribute labels
are displayed. Default is TRUE.}

\item{vector_scale}{Numeric or NULL. Scaling factor for loading vectors
in a biplot. If NULL, a suitable value is calculated automatically.}

\item{show_origin}{Logical. If TRUE, horizontal and vertical reference
lines are shown at zero. Default is TRUE.}
}
\value{
A ggplot object.
}
\description{
Creates a PCA score plot, loading plot, or combined sensory biplot
from an object returned by `sensory_pca()`.
}
\details{
The `"scores"` plot displays products in PCA space.

The `"loadings"` plot displays sensory attribute loading coordinates.

The `"biplot"` combines product scores and loading vectors. Loading
vectors are rescaled only for visualization; the underlying PCA
loadings are not altered.

Axis labels automatically report the percentage of total variance
explained by each selected principal component.
}
\examples{
\dontrun{
pca_result <- sensory_pca(
  data,
  attributes = c(
    "sweetness",
    "bitterness",
    "firmness"
  )
)

sensory_pca_plot(
  pca_result,
  type = "biplot"
)
}

}
